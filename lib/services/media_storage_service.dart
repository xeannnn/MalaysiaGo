/// Photo uploads are disabled in the free/Spark build.
///
/// MalaysiaGo still displays profile/image URLs that already exist, but new
/// uploads are intentionally not offered because Cloud Storage is not enabled.
class MediaStorageService {
  const MediaStorageService();
}
